 <nav>
  <%= link_to 'MentorMe!', root_path, id: "main-title" %>
  <% if !user_signed_in? %>
  <%= link_to 'Sign In', new_user_session_path %>
  <%= link_to 'Register', new_user_registration_path %>
  <%end%>
  <%if user_signed_in?%>
  <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
  <span id="type"><%= current_user.type %> Account</span>
  <%end%>

</nav>


<h1 id="welcome">Yo, <%= link_to "#{current_user.username}", root_path%>!</h1>